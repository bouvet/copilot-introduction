# Measure Validation Prompts

Use these prompts with Copilot Chat after generating DAX:

- "Create three test scenarios for `Margin %` including expected outputs."
- "What edge cases could break `Revenue YoY %`?"
- "Suggest a validation checklist for `North Revenue` with slicers on Region and Date."
- "Generate a DAX query to sanity-check `Total Revenue` by month."
- "How do I validate `Enterprise Revenue` across Customers[Segment] filters?"
- "Create a test plan to validate `Product Revenue Rank` with Product and Region slicers."
